/*#include <stdio.h>
#define MAX 5

int main(void)
{
	int i, tmp = 0, what = 0;

	for (i = 1; i <= MAX; i++)
	{
		tmp += i;
		printf("%d", tmp);
		what += tmp;
	}
	printf("\n무엇일까요? %d\n", what);

	return 0;

}*/
